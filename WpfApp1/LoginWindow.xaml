<Window x:Class="WpfApp1.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Title="Авторизация - Жар-птица" Height="350" Width="450">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Вход в систему" FontSize="24" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" VerticalAlignment="Center">
            <TextBlock Text="Логин" FontSize="14" Margin="0,0,0,5"/>
            <TextBox x:Name="LoginTextBox" Height="35" Margin="0,0,0,15" FontSize="14"/>

            <TextBlock Text="Пароль" FontSize="14" Margin="0,0,0,5"/>
            <PasswordBox x:Name="PasswordBox" Height="35" Margin="0,0,0,15" FontSize="14"/>

            <Button x:Name="LoginButton" Content="Войти" Height="40" FontSize="16" 
                    Background="#3498DB" Foreground="White" Cursor="Hand"
                    Click="LoginButton_Click" Margin="0,10,0,0"/>

            <Button x:Name="BackButton" Content="Назад" Height="35" FontSize="14" 
                    Background="#95A5A6" Foreground="White" Cursor="Hand"
                    Click="BackButton_Click" Margin="0,10,0,0"/>
        </StackPanel>

        <TextBlock x:Name="ErrorText" Grid.Row="2" Foreground="Red" 
                   TextAlignment="Center" Margin="0,10,0,0" Visibility="Collapsed"/>
    </Grid>
</Window>
